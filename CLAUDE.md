# CLAUDE.md

이 프로젝트에서 Claude가 지켜야 할 규칙. 모든 응답은 **한국어**로 한다.

---

## 0. 이 프로젝트의 목적

**게임 클라이언트 개발자 신입 취업용 포트폴리오.**

이 한 줄이 모든 판단의 기준이다. 목표는 "돌아가는 게임"이 아니라
**"면접에서 방어 가능한 게임"** 이다. 둘이 충돌하면 후자를 택한다.

- 개발자: 1인 (소규모 게임 제작 경험 있음)
- 엔진: **Unity 6.3 LTS (6000.3.9f1)** / C# — 고정. 변경하지 않는다.
- 기간: 약 3~4개월
- 문서 체계: **[docs/README.md](docs/README.md)** — 어떤 문서가 어디 있는지는 여기만 본다.
- 현재 상태: **[docs/progress/STATUS.md](docs/progress/STATUS.md)**

---

## 1. 역할 분담 — 가장 중요한 규칙

**포트폴리오는 본인이 설명할 수 있어야만 가치가 있다.**
설명하지 못하는 코드는 있으나 마나가 아니라 **마이너스**다.
면접에서 한 번 막히면 프로젝트 전체의 신뢰도가 무너진다.

### 사용자가 직접 작성한다 (Claude는 코드를 쓰지 않는다)

면접 단골 질문이 나오는 영역이다.

- 플레이어 컨트롤러 / 입력 처리
- 전투 로직 (데미지 계산, 히트 판정)
- 적 AI 상태 머신 (FSM)
- 오브젝트 풀링
- 스탯 / 모디파이어(버프·디버프) 시스템
- 절차적 방 생성 알고리즘
- 세이브 / 로드 직렬화

**이 영역을 요청받으면 Claude는 코드 대신 이 순서로 답한다.**

```
1. 설계안 제시 (선택지가 여럿이면 트레이드오프와 함께)
2. 클래스·메서드 시그니처와 책임 분리까지만 제시
3. 의사코드 또는 힌트
4. → 사용자가 구현
5. 구현물을 리뷰 (동작 + 설계 + 면접 예상 질문)
```

막혀서 명시적으로 요청하면 그때 **막힌 부분만** 최소한으로 제공한다.
전체를 대신 써주지 않는다.

### Claude가 작성해도 되는 것

실력 증명과 무관한 반복 작업이다.

- UI 바인딩 / 레이아웃 보일러플레이트
- ScriptableObject 클래스 껍데기 (필드 정의)
- 에디터 툴 / 커스텀 인스펙터
- 씬 로더, 상수, 열거형
- 테스트 코드 골격
- 디버그 유틸리티
- 문서 (README, 기획서, 기술 문서)

### 항상 붙일 것

코드만 던지지 않는다. 어떤 코드든 **왜 이렇게 했는지와 검토한 대안**을 함께 설명한다.
이 설명이 곧 면접 답변의 초안이다.

---

## 2. 생각하고 코딩하라

**추측하지 마라. 혼란을 숨기지 마라. 트레이드오프를 드러내라.**

- 가정은 명시한다. 불확실하면 묻는다.
- 해석이 여럿이면 전부 제시한다. 조용히 하나 고르지 않는다.
- 더 단순한 방법이 있으면 말한다. 필요하면 반대한다.
- 불명확하면 멈춘다. 무엇이 헷갈리는지 이름 붙여 말하고 묻는다.

---

## 3. 단순함 우선 — 단, 포트폴리오 예외가 있다

**문제를 푸는 최소한의 코드. 추측성 코드 금지.**

- 요청받지 않은 기능 금지
- 한 번만 쓰는 코드에 추상화 금지
- 요청하지 않은 "유연성", "설정 가능성" 금지
- 일어날 수 없는 상황에 대한 예외 처리 금지
- 200줄을 짰는데 50줄로 가능하면 다시 써라

기준: **"시니어 엔지니어가 이걸 과설계라고 할까?"** 그렇다면 단순화한다.

### 예외 — 이 프로젝트가 포트폴리오이기 때문에

다음은 "지금 당장 필요한가"가 아니라 **"보여줄 가치가 있는가"** 로 판단한다.
단, 예외를 적용할 때는 **반드시 그렇게 한다고 밝히고 이유를 말한다.**

- **데이터 주도 설계**: 적이 1종뿐이어도 ScriptableObject로 분리한다.
  확장성 증명이 목적이다.
- **최적화**: 지금 성능 문제가 없어도 풀링과 프로파일링을 한다.
  전후 수치가 곧 증거물이다.
- **테스트**: 순수 로직에는 유닛테스트를 작성한다.

이 예외는 위 세 가지에만 적용한다. 나머지는 전부 3번 원칙을 따른다.

---

## 4. 외과적 변경

**꼭 건드려야 할 것만 건드린다. 내가 만든 것만 치운다.**

- 주변 코드, 주석, 포매팅을 "개선"하지 않는다.
- 망가지지 않은 것을 리팩터링하지 않는다.
- 기존 스타일을 따른다. 내 취향과 달라도.
- 관련 없는 죽은 코드를 발견하면 **말만 하고 지우지 않는다.**
- 내 변경 때문에 안 쓰이게 된 import·변수·함수는 내가 지운다.

테스트: **바뀐 모든 줄이 사용자 요청으로 직접 추적되는가.**

---

## 5. 검증 — Unity에 맞게 재정의

Unity 게임플레이 코드는 대부분 자동 테스트가 불가능하다.
따라서 "테스트를 통과시켜라"를 그대로 적용할 수 없다. 3단으로 나눈다.

```
1단 (Claude) : 컴파일 통과 + null/참조 오류 점검
2단 (Claude) : "플레이모드에서 확인할 것" 체크리스트 제시
3단 (사용자) : 에디터에서 체크리스트 확인 후 결과 회신
```

**작업 전에 성공 기준을 먼저 합의한다.** "동작하게 만들기"는 기준이 아니다.

체크리스트 예시:

```
- [ ] 적이 플레이어를 감지 범위 안에서만 추적하는가
- [ ] 감지 범위를 벗어나면 원위치로 복귀하는가
- [ ] 추적 중 플레이어가 죽으면 Idle로 전이하는가
```

**EditMode 유닛테스트를 실제로 작성하는 대상** (MonoBehaviour 의존 없는 순수 로직):

- 스탯 계산 및 모디파이어 중첩
- 데이터 파싱 / 검증
- 절차적 생성 규칙 (연결성, 방 개수 보장 등)

다단계 작업은 착수 전 계획을 밝힌다.

```
1. [단계] → 검증: [확인 방법]
2. [단계] → 검증: [확인 방법]
```

---

## 6. Unity / C# 규칙

### 금지

- `Update()` 안에서 `GetComponent`, `Find`, `Camera.main` 호출
  → `Awake`에서 캐싱
- `GameObject.Find` / `FindObjectOfType` 상시 사용
  → `[SerializeField]` 인스펙터 참조 우선, 초기화 1회만 예외 허용
- **`FindObjectOfType` / `FindObjectsOfType` 자체가 Unity 6에서 폐기됨**
  → 꼭 써야 하면 `FindFirstObjectByType` / `FindAnyObjectByType`을 쓴다.
- `public` 필드로 인스펙터 노출 → `[SerializeField] private` 사용
- 매 프레임 `Instantiate` / `Destroy` → 오브젝트 풀링
- 문자열 기반 API: `SendMessage`, `Invoke("이름")`, `Animator.Play("이름")`
  → 애니메이터는 `Animator.StringToHash`로 캐싱
- **Unity 오브젝트에 `?.` `??` `?? =` 사용**
  → Unity의 fake null과 충돌해 파괴된 오브젝트를 살아있다고 판단한다.
    반드시 `if (obj == null)`로 비교한다. *(면접 단골 질문)*
- `Update` 경로에서 LINQ 및 람다 캡처 → GC Alloc 유발
- 릴리스 경로에 `Debug.Log` 잔류

### 준수

- 코루틴은 시작한 곳이 정리 책임을 진다. `OnDisable`에서 중단 처리.
- 이벤트 구독(`+=`)은 반드시 해제(`-=`)와 짝을 이룬다.
- UI는 매 프레임 폴링하지 않고 **이벤트로 갱신**한다.
- 씬 간 데이터 전달에 static 남용 금지.

### 네이밍

| 대상 | 규칙 | 예 |
|---|---|---|
| 클래스 / 메서드 | PascalCase | `EnemyController`, `TakeDamage` |
| private 필드 | `_camelCase` | `_currentHp` |
| public 프로퍼티 | PascalCase | `CurrentHp` |
| 상수 | PascalCase | `MaxHealth` |
| 인터페이스 | `I` + PascalCase | `IDamageable` |
| ScriptableObject 에셋 | `타입_이름` | `EnemyData_Slime` |

---

## 7. 프로젝트 구조

```
Assets/
├─ _Project/          ← 내가 만든 것
│  ├─ Scripts/  { Player, Enemy, Combat, Data, UI, Core }
│  ├─ Prefabs/
│  ├─ ScriptableObjects/
│  ├─ Scenes/
│  ├─ Art/
│  └─ Audio/
└─ ThirdParty/        ← 외부 에셋 (내 코드와 절대 섞지 않는다)
```

`_Project`와 `ThirdParty` 분리는 **면접관이 "어디까지가 본인 코드인지"를
즉시 알 수 있게** 하기 위한 것이다. 이 경계를 흐리는 제안을 하지 않는다.

### 외부 라이브러리 · 에셋 정책

- **핵심 시스템은 직접 구현한다.** (1번의 사용자 작성 목록 = 외부 사용 금지 목록)
- 아트 / 사운드 / 폰트, 그리고 DOTween 같은 주변 유틸리티는 허용한다.
- 사용한 모든 외부 리소스는 **README의 출처 표에 즉시 기록**한다.
  누락은 "직접 만든 척한다"는 최악의 인상을 준다.
- 새 외부 의존성 도입을 제안할 때는 **직접 구현 대비 이득**을 먼저 말한다.

---

## 8. 커밋 규칙

**커밋 히스토리 자체가 포트폴리오다.** 한 번에 몰아 올린 저장소는 의심받는다.

- 기능 단위로 쪼개서 자주 커밋한다. 최소 주 1회.
- 형식: `타입: 한글 설명`
  타입은 `feat` `fix` `refactor` `perf` `test` `docs` `chore`
  - 예: `feat: 적 추적 상태 구현`
  - 예: `perf: 투사체 오브젝트 풀링 적용으로 GC Alloc 제거`
- **커밋과 푸시는 사용자가 요청할 때만 한다.** 임의로 하지 않는다.

---

## 9. 문서 운영

문서 체계와 그 규칙은 **[docs/README.md](docs/README.md)** 에 있다.
이 파일에는 적지 않는다. 문서 구조가 바뀌어도 CLAUDE.md는 그대로여야 하기 때문이다.

Claude가 지킬 것은 세 가지다.

**작업 시작 전** — `docs/progress/STATUS.md`를 읽는다. 지금 어디까지 왔는지 여기 있다.

**기능 착수 전** — `docs/specs/`에 명세를 만든다. `_TEMPLATE.md`를 복사해서 쓴다.
후보 단계인 기능은 명세를 만들지 않는다. `docs/design/feature-pool.md`에 둔다.
**만들기로 확정한 것만 파일이 생긴다.**

**작업 종료 후** — `STATUS.md`를 갱신한다. 덮어쓴다. 이력은 git이 갖는다.
설계 판단이 있었으면 `docs/decisions/`에 파일을 추가한다.

### 문서를 건드릴 때의 금지 사항

- 손으로 관리하는 목차·인덱스를 만들지 않는다. **폴더 목록이 인덱스다.**
- 파일명에 `F01-` 같은 순번을 붙이지 않는다. 삭제와 재정렬에 약하다.
- 폐기된 기능 명세를 삭제하지 않는다. `status: dropped`로 바꾼다.
- 이미 쓴 `docs/decisions/` 파일을 수정하지 않는다. 새 파일을 쓰고 `superseded_by`로 잇는다.
- 문서에 변경 이력을 손으로 적지 않는다. 문서에는 **지금의 사실**만 적는다.
