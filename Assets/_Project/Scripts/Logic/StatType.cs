namespace JM2D.Logic
{
    /// 스탯을 값으로 지목한다. 데이터 에셋이 어느 스탯을 건드리는지 적을 때 쓴다.
    /// 프로퍼티는 이름이라 에셋에 담기지 않지만 열거형은 담긴다.
    public enum StatType
    {
        MaxHealth,
        MoveSpeed,
        AttackDamage,
        AttackSpeed
    }
}
